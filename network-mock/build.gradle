version '1.0'

test {
    filter {
        includeTestsMatching "*.NetworkMockSuite"
    }
}

dependencies {
    compile group: 'org.mockito', name: 'mockito-core', version: '1.10.19'
    compile project(":network-api");
}
